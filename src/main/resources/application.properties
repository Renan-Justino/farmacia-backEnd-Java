spring.application.name=farmacia-api

# JPA - Configura��o base
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Seguran�a (JWT)
# AVISO: esta chave é apenas para desenvolvimento. Substitua por um segredo forte em produção e gerencie via variáveis de ambiente/secret manager.
jwt.secret=dev-secret-CHANGE_ME_please_use_a_secure_key_at_prod_least_32_chars
jwt.expiration=3600000
